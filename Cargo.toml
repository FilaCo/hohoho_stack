[package]
name = "hohoho_stack"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[dependencies]
bevy = { version = "0.15", features = ["wayland"] }
bevy_asset_loader = { version = "0.22" }
bevy_fluent = { version = "0.11" }
